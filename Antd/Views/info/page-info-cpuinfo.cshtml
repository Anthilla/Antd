<legend id="Cpuinfo">Cpuinfo</legend>
<table id="cpuinfo" class="table">
    <thead>
        <tr>
            <th class="text-left" style="width: 100px;" rowspan="2">CPUINFO</th>
            <th class="text-left">&nbsp</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Antd.Models.CpuinfoModel row in Model.CPUINFO) {
            <tr>
                <td class="right">@row.key</td>
                <td class="right">@row.value</td>
            </tr>
        }
    </tbody>
</table>
